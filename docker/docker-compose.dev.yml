services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: jinsei
      POSTGRES_PASSWORD: jinsei
      POSTGRES_DB: jinsei
    ports:
      - "5431:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
